# Test Patterns

Paste reusable test commands, fixtures, expected failure patterns, browser checks, and known-good examples here.

The local worker treats this as guidance, not permission to run arbitrary commands. Commands still have to match `worker/config.json` allowlists.

